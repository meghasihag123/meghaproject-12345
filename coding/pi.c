#include <stdio.h>
#include <math.h> // use headerfile
#include <pi.h>   // use headerfile
/*double sine();
int main()
{
    sine();
    return 0;
}
double sine()
{
    double angle_degrees;
    printf("Enter angle in degrees ");
    scanf("%lf", &angle_degrees);
    double j = pi(3.141592653589793);
    // printf("%lf", j);
    double angle_radians = angle_degrees * (j / 180); // CONVERT DEGREES TO RADIANS

    double sin_value = sin(angle_radians);

    printf("The sine of %.2lf is : %.2lf\n", angle_degrees, sin_value);
}*/
// SHORT program
/*double sine()
{
    double angle_degrees;
    printf("Enter angle in degrees ");
    scanf("%lf", &angle_degrees);
    // double j = pi(3.141592653589793); // or 3.141593
    double j = pi(3.141593);
    printf("pi = %lf\n", j);
    double angle_radians = angle_degrees * (j / 180); // CONVERT DEGREES TO RADIANS

    double sin_value = sin(angle_radians);

    printf("The sine of %.2lf is : %.2lf\n", angle_degrees, sin_value);
}
int main()
{
    sine();
    return 0;
}*/
